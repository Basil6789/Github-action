name : First Workflow 

on:
  push:
    branches:
      -main

jobs:
  example-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message
        run: echo "welcome to my place"
        
      
    
